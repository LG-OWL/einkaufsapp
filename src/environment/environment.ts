export const environment = {
    production: false,
    firebase: {
        apiKey: "api",
        authDomain: "domain",
        databaseURL: "url",
        projectId: "id",
        storageBucket: "bucket",
        messagingSenderId: "senderid"
    }
};